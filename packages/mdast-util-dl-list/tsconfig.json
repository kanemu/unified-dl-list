{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",

    /* If you use node:test or Node globals in source/tests, keep this.
       If not needed, you can remove it. */
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "src/**/*.d.ts"]
}
